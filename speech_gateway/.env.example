# Optional client authentication for the gateway itself.
SPEECH_GATEWAY_API_KEY=

# Optional path; defaults to speech_gateway/config.yaml.
SPEECH_GATEWAY_CONFIG=

VOLCANO_APPID=
VOLCANO_ACCESS_TOKEN=
VOLCANO_DEFAULT_VOICE=
VOLCANO_CLUSTER=volcano_icl

# Example secret for a user-managed OpenAI-compatible local TTS service.
LOCAL_TTS_API_KEY=
